[build]
  command = "npm run build"
  publish = "dist"
  base = "/"

[build.environment]
  NODE_VERSION = "18"

[context.branch-deploy]
  command = "npm run build"
  publish = "dist"
  base = "/"

[context.production]
  command = "npm run build"
  publish = "dist"
  base = "/"
